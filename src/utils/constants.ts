export enum Routes {
    AUTH = 'auth',
    USER = 'user',
    CONVERSATIONS = 'conversations',
}

export enum Names {
    JWT_TOKEN_AUTH = 'JWT_token_auth',
}

export enum Lengths {
    PASSWORD_MIN_LEN = 6,
}